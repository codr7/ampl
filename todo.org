* add Form pop_front(deque<Form>)
** use move/front/pop_front
** grep pop_front and replace
*** libs
* run bench unsafe for comparison
* add d call flag
* add let macro
* add iter
* add for macro
* add stack reader
* add string type
* add char type
* eval func args/rets in macro
